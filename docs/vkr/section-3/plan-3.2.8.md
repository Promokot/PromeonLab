# План пункта 3.2.8 «Экспорт данных»

Статус: УТВЕРЖДЁН с решениями (2026-06-05), текст написан (3.2.8.md, черновик v1).
База: plan-3.2.md (состав утверждён), outline-section-3.md (G), код Assets/_App/Scripts/ExportPipeline/ + SpatialUi/Panels/ExportPanel.cs.
ВАЖНО: пункт написан ВНЕ ОЧЕРЕДИ (3.2.5–3.2.7 не написаны). Нумерация – ПЛЕЙСХОЛДЕРЫ 3.Э-N / Б.Э-N (решение 1), реальные номера присваиваются при перенумерации после 3.2.5–3.2.7. В numbering-registry.md до перенумерации НЕ вносится.

## Проверка по коду (сводка фактов)

- Модуль из четырёх файлов: SceneExporter (267 строк), SceneBundle (59), события SceneExportRequestedEvent (8) и SceneExportedEvent (10). Панель – ExportPanel (125), модуль пользовательской панели на вкладке exporter панели навигации.
- SceneExporter – объект корневой области жизни (IStartable): подписан на SceneExportRequestedEvent, по завершении публикует SceneExportedEvent (путь, успех, сообщение). Запуск только через события; панель дополнительно обращается к BuildTargetPath для подписи пути (имя очищается от недопустимых символов, пустое заменяется на export).
- ExportPanel: поле имени файла (по умолчанию подставляется имя сцены), подпись вычисленного пути, имя активной сцены, кнопка экспорта (блокируется на время операции), строка статуса из события результата. Реагирует на SceneContextChangedEvent.
- Порядок экспорта (RunExportAsync): проверка SceneContext.HasScene → на главном потоке снимок: CaptureSnapshot графа + CaptureForExport анимационных данных (в песочнице анимационная часть опускается) → разрешение источников → чистая статическая BuildBundle → JsonUtility → запись ZIP на фоновом потоке (Task.Run). Ошибки перехватываются, публикуется событие с неуспехом.
- Разрешение источника (Resolve): по AssetRef запись находится в AssetRegistry, относительный SourceRef превращается в абсолютный путь, проверяется существование файла.
- BuildBundle – чистая функция (вход: SceneData, SceneAnimationData, делегат разрешения, метка времени; выход: SceneBundle + список файлов-источников). Нода: трансформация, позы костей, блок анимации контейнера владельца (кадры, интерполяция, цикл, треки; треки костей – префикс bone:). Импортированные с существующим источником → models/{assetId}.{ext} или textures/… (Reference → textures), дедупликация по пути в архиве; встроенные и потерянные источники → geometryMissing.
- SceneBundle – внешняя схема scene.json (schemaVersion 1, метка времени, ссылка на сцену, fps, ноды). Односторонний снимок, НЕ формат обратного импорта. Переиспользует BonePose и AnimKeyData.
- WriteZipBundle: без Unity API (безопасно на фоновом потоке), ZipArchive из System.IO.Compression; scene.json текстом, источники потоково. Путь: Documents/{Application.productName}/{имя}.zip – вне локального хранилища (результат для пользователя).
- Сообщение результата: путь + счётчик нод без геометрии.
- ⚠ EditMode-тесты модуля (SceneBundleTests, SceneExporterBuildTests, SceneExporterZipTests) в тексте НЕ описываются (решение F5).
- ОГОВОРКА МАКСА (2026-06-05): формат выведен, но для чтения пакета сторонним редактором (например, Blender) нужен мост-аддон на принимающей стороне; его разработка выходит за рамки задач ВКР, поэтому не выполнялась. Нейтрально, как ограничение и направление развития.

## Структура текста (как написано)

1. Вводный абзац: экспорт – завершающий этап работы со сценой; состав модуля (панель + экспортёр); событийная связка запрос/результат.
2. Панель экспорта: поля, обновление по смене сцены, путь через BuildTargetPath, блокировка кнопки, статус. [Рисунок 3.Э1 СКРИНШОТ; Листинг 3.Э1 фрагмент ExportPanel; полный – Б.Э1]
3. Снимок состояния: HasScene, CaptureSnapshot + CaptureForExport на главном потоке, разрешение источников через AssetRegistry. [полный SceneExporter – Б.Э2]
4. BuildBundle: чистая функция, перенос нод, отбор и дедупликация источников, geometryMissing. [Листинг 3.Э2 фрагмент]
5. Внешняя схема SceneBundle: состав корня, адресация треков, переиспользование BonePose/AnimKeyData, односторонность. [Листинг 3.Э3 целиком, в Б не дублируется]
6. Запись ZIP: фоновый поток, ZipArchive [ИСТ], путь в Documents, сообщение результата. [Листинг 3.Э4 фрагмент; Рисунок 3.Э2 СКРИНШОТ состава пакета]
7. Финал: собственная схема ≠ обменный формат → мост-аддон (пример Blender) вне рамок работы, направление развития; закрытие рабочего цикла приложения.

## РЕШЕНИЯ (2026-06-05, Макс, интерактив)

1. Нумерация черновика – ПЛЕЙСХОЛДЕРЫ без номеров (3.Э1…, Б.Э1…); номера присвоятся при перенумерации после 3.2.5–3.2.7; в реестр пока не вносим.
2. Рисунок экспортированного файла – ОДИН рисунок: скриншот содержимого ZIP в проводнике с пояснениями состава («вот так в проводнике, вот это там лежит»). Отдельная схема состава пакета НЕ делается – объединена со скриншотом.
3. Мост-аддон – финальный абзац пункта, с примером Blender, нейтрально (ограничение + направление развития).
4. Листинги сверх базовых: фрагмент ExportPanel (запуск + путь) и фрагмент WriteZipBundle – ДА; события целиком – НЕТ.

## Итоговый состав (плейсхолдеры)

Листинги: 3.Э1 – ExportPanel.cs (Фрагмент запуска экспорта и обновления пути); 3.Э2 – SceneExporter.cs (Фрагмент сборки пакета); 3.Э3 – SceneBundle.cs (целиком, в Б не дублируется); 3.Э4 – SceneExporter.cs (Фрагмент записи архива).
Рисунки: 3.Э1 – Панель экспорта сцены (СКРИНШОТ, Макс); 3.Э2 – Состав экспортированного ZIP-пакета (СКРИНШОТ проводника с пояснениями, Макс; аннотации добавляются при сборке рисунка).
Приложение Б (по порядку упоминания): Б.Э1 – ExportPanel.cs (полный); Б.Э2 – SceneExporter.cs (полный).

## Правки Макса (ревью v1, 2026-06-05) – зафиксированы как решения

1. Вводный абзац разбит на два предложения; «собранная сцена вместе с объектами и анимациями».
2. «кнопка запуска экспорта блокируется» (уточнение).
3. Абзац BuildBundle: «чистая функция» → «выполняет единственную функцию»; «треками с ключами» → «трек-дорожки с ключами»; длинное предложение про каталоги разбито точкой; УДАЛЕНО предложение «Так же помечаются записи с утраченным источником» (случай утраченного источника в тексте не упоминается).
4. ⚠ Замечание Макса: абзац о SceneBundle (перед Листингом 3.Э3) дублирует уже описанную логику BuildBundle – урезать/склеить или пересмотреть структуру. Решение – см. ниже.
5. Мелкие хвосты правок (чинятся при перезаписи): потерянное двоеточие «снимок состояния: метод», двойной пробел/перенос в первом абзаце, согласование падежей «включающим количество кадров, тип интерполяции...». Термин «трек-дорожки» принят как правка Макса (заметка: тавтологичен, track = дорожка – решить при вычитке).

## РЕШЕНИЕ 5 (2026-06-05, Макс, интерактив) – структура v2

Абзац схемы SceneBundle дублировал абзац BuildBundle. Выбран вариант «схема → сборка»: сначала «что» (SceneBundle: корень, состав ноды, адресация трек-дорожек, переиспользование BonePose/AnimKeyData), затем «как» (BuildBundle: заполнение по снимку, отбор файлов, дедупликация, geometryMissing – БЕЗ повтора состава ноды). Односторонность схемы перенесена в финальный абзац (рядом с мостом-аддоном). Листинги поменялись местами: 3.Э2 – SceneBundle.cs (целиком), 3.Э3 – SceneExporter.cs (Фрагмент сборки пакета). Текст – ЧЕРНОВИК v2.

## Правки Макса (ревью v2) и СТАТУС

Правки v2 (зафиксированы как решения):
1. Два предложения вместо двоеточия: «методом BuildTargetPath экспортёра.» и «Запись архива вынесена с главного потока.»
2. «запись библиотеки – хранимая в ней...» (тире вместо двоеточия).
3. УДАЛЕНО предложение об адресации трек-дорожек (идентификатор целевой ноды, префикс bone) – в тексте не описывается.
4. «Геометрию ноды задают ссылка на файл внутри архива ИЛИ ФЛАГ её отсутствия geometryMissing».
5. BuildBundle: «на выходе формируется заполненный пакет»; встроенные ассеты «не имеют исходного файла с идентификатором, так как состоят из заготовленных префабов».
6. Починено при фиксации: «индентификатором» → «идентификатором», «т.к» → «так как» (правило 2.7 – без сокращений).

СТАТУС: ПРИНЯТ / ЗАСЧИТАН КАК ВЫПОЛНЕННЫЙ (2026-06-05, финальная редакция Макса v3, вне очереди).
Хвосты до финальной сборки: перенумерация плейсхолдеров 3.Э-N/Б.Э-N после написания 3.2.5–3.2.7 (тогда же – внесение в numbering-registry.md); скриншоты Рисунков 3.Э1 и 3.Э2 (Макс); маркер [ИСТ: System.IO.Compression] при подборе источников; заметка на вычитку – термин «трек-дорожки» тавтологичен.
