# Детальный план пункта 3.2.3 «Главное меню и управление сценами»

Статус: УТВЕРЖДЁН Максом (2026-06-05), все вопросы закрыты – см. блок «Решения Макса» в конце файла.
База: plan-3.2.md (блок 3.2.3) + перечитанные скрипты MainMenuPanel (66), ScenePickerPanel (81), SceneListNode_Item (31), SceneGraph (224), SceneNode (39), OutlinerPanel (158), OutlinerNode_Item (37), OutlinerNode_Rig_Item (17).
Свободные номера: Листинг с 3.26, Рисунок с 3.8, Таблица с 3.3, Приложение с Б.10. Точные номера – при написании.

## Предмет и связки

Сценарий запуска и жизненный цикл сцены глазами пользователя: список сцен → создание/удаление/выбор → открытие → построение рантайм-графа → обзор иерархии. Обратные ссылки: панели меню регистрируются через RegisterComponentInHierarchy (см. Листинг 3.13 в 3.2.1); SceneListNode_Item и OutlinerNode_Item – элементы списков (3.2.2, Таблица 3.2); загрузка SceneData и кеш – 3.1.1; CaptureSnapshot – пара к автосохранению 3.1.4. Вперёд (без номеров): восстановление геометрии через реестр билдеров – тема ассетов; выбор и переименование нод – тема взаимодействия.

## Состав (по порядку изложения)

1. Вводный абзац: режим главного меню как точка входа; две панели, обе – внутрисценные объекты области MainMenuSceneScope.
2. ScenePickerPanel – управление набором сцен:
   - список из AppStorage.GetAllScenesAsync, строки из префаба SceneListNode_Item (Init/SetSelected, событие Clicked);
   - создание: имя из поля ввода (через VrKeyboard/мост из 3.2.2 – одной фразой), CreateSceneAsync (пара к Листингу 3.2);
   - удаление выбранной, перестройка списка; выбор → сообщение SceneSelectedEvent.
   - КОД: Листинг 3.26 – фрагмент ScenePickerPanel.cs (RefreshAsync + SpawnItem + OnItemClicked, ~30 строк).
   - ПРИЛОЖЕНИЕ: Б.10 – ScenePickerPanel.cs (полный). ВОПРОС: SceneListNode_Item целиком в текст (31 строка, образцовый элемент списка) или только в Б (Б.11)?
3. MainMenuPanel – открытие:
   - слушает SceneSelectedEvent (кнопка Open неактивна без выбора, подпись с именем сцены);
   - открытие: LoadSceneAsync → SetActiveScene → SceneOpenedEvent → TransitionTo(VrEditing) – связка с оркестратором 3.2.1;
   - песочница: BeginSandboxSession → Sandbox (временная сцена из 3.1.1).
   - КОД: Листинг 3.27 – фрагмент MainMenuPanel.cs (OnOpenSandbox + OpenSceneAsync, ~20 строк).
   - ПРИЛОЖЕНИЕ: Б.12 – MainMenuPanel.cs (полный).
4. SceneGraph – рантайм-граф сцены:
   - внутрисценный объект (IStartable), подписан на SceneOpenedEvent; корень [Spawned];
   - OnSceneOpenedAsync: очистка → чтение SceneData → для каждой записи поиск ассета в реестре и восстановление объекта (словесно, без углубления) → SceneNode.Init → второй проход восстановления иерархии родительства → применение поз костей (отсылка к 3.1.2); устойчивость к отсутствующим ассетам (предупреждение, узел пропускается);
   - SceneNode – компонент-паспорт ноды (идентификатор, ссылка на ассет, имя, видимость, блокировка);
   - CaptureSnapshot – обратное преобразование графа в SceneData (его потребитель – автосохранение, 3.1.4).
   - КОД: Листинг 3.28 – фрагмент SceneGraph.cs (OnSceneOpenedAsync, ~45 строк, комментарии опущены). ВОПРОС: CaptureSnapshot отдельным листингом (~30 строк) или словами (метод уже упоминался в 3.1.4)? ВОПРОС: SceneNode целиком (39 строк) или словами?
   - ПРИЛОЖЕНИЕ: Б.13 – SceneGraph.cs (полный), Б.14 – SceneNode.cs (если не в тексте).
5. OutlinerPanel – обозреватель иерархии:
   - постоянная панель региона; реагирует на SceneContextChangedEvent/SceneModifiedEvent (перестройка), SelectionChangedEvent (подсветка), NodeRenamedEvent, BonesVisibilityChangedEvent (точечные обновления без перестройки);
   - Rebuild: группировка нод по родителю, обход в глубину с отступами, два префаба строк – обычная и риг-строка (OutlinerNode_Item / OutlinerNode_Rig_Item);
   - детали выбора и работы с костями – вперёд, без номеров.
   - КОД: Листинг 3.29 – фрагмент OutlinerPanel.cs (Rebuild + обход, ~30 строк).
   - ПРИЛОЖЕНИЕ: Б.15 – OutlinerPanel.cs (полный).
6. Замыкание цикла: возврат в меню кнопкой UserPanel (3.2.2) → ModeExitingEvent → автосохранение (3.1.4) – один абзац, сцена «переживает» сеанс.

## Рисунки

- Рисунок 3.8 – СКРИНШОТ: главное меню с пикером сцен (ВОПРОС: одним кадром меню+пикер или двумя рисунками 3.8/3.9?).
- Рисунок 3.9 – СКРИНШОТ: обозреватель иерархии с объектами и раскрытым ригом.
- ВОПРОС: нужна ли СХЕМА потока открытия сцены (SceneSelectedEvent → SceneOpenedEvent → переход → построение графа)? Если да – Рисунок 3.10.

## Вопросы Максу (задать в начале сессии)

1. SceneListNode_Item: целиком в текст или только в Приложение Б?
2. CaptureSnapshot: листинг или словами?
3. SceneNode: целиком в текст или словами + Б?
4. Скриншоты меню и пикера: один кадр или два?
5. Схема потока открытия сцены: нужна ли?
6. Бюджет пункта: 4 листинга в тексте (3.26–3.29) – норм или ужать до 3?

## Решения Макса (2026-06-05)

1. SceneListNode_Item (31 строка) – ЦЕЛИКОМ В ТЕКСТ (правило ≤60 строк), в Приложение Б не дублируется.
2. CaptureSnapshot – СЛОВАМИ (метод уже фигурировал в 3.1.4; полный SceneGraph.cs идёт в Б).
3. SceneNode (39 строк) – ЦЕЛИКОМ В ТЕКСТ, в Приложение Б не дублируется.
4. Скриншоты меню и пикера – ОДНИМ КАДРОМ (Рисунок 3.8); обозреватель – Рисунок 3.9.
5. Схема потока открытия сцены – НЕ НУЖНА, поток описывается словами («переходов полтора землекопа»).
6. Бюджет – 6 листингов в тексте норм.

Итоговая нумерация (внесена в numbering-registry.md):
- Листинги в тексте: 3.26 ScenePickerPanel (фрагмент), 3.27 SceneListNode_Item (целиком), 3.28 MainMenuPanel (фрагмент), 3.29 SceneGraph (фрагмент OnSceneOpenedAsync), 3.30 SceneNode (целиком), 3.31 OutlinerPanel (фрагмент Rebuild).
- Приложение Б: Б.10 ScenePickerPanel.cs, Б.11 MainMenuPanel.cs, Б.12 SceneGraph.cs, Б.13 OutlinerPanel.cs.
- Рисунки: 3.9 меню+пикер одним кадром, 3.10 обозреватель иерархии. Таблиц нет.

## Дополнение (2026-06-05, после черновика)

7. Обратные отсылки на прошлые пункты/листинги ПРОРЕДИТЬ: оставлены две (цепочка перехода из 3.2.1, автосохранение из 3.1.4), остальные сняты. Общее правило для дальнейших пунктов – см. REQUIREMENTS 5.13.
8. Новый Рисунок 3.6 «Виды сцен трёх режимов приложения» (коллаж из трёх кадров, готовит Макс) вставлен в 3.2.1 после графа переходов; рисунки 3.6–3.9 сдвинуты в 3.7–3.10 (правки внесены в 3.2.2 и черновик 3.2.3).
9. Блок после Рисунка 3.9 пересобран (2026-06-05, вариант A-бридж): загрузка (бридж от перехода, Листинг 3.29 SceneGraph) → SceneNode (3.30) → SceneGraph-определение (учёт нод, [Spawned], порядок сообщений, SetActiveScene) → обозреватель (3.31, Рисунок 3.10) → CaptureSnapshot → финал. Цепочка перехода сжата до фразы со ссылкой на 3.2.1. Нумерация листингов не изменилась.
10. Финальная структура блока (2026-06-05, отшлифована интерактивно): граф SceneGraph (представление: учёт нод, умения, [Spawned], Б.12) → нода с паспортом (Листинг 3.29 = SceneNode.cs) → запуск загрузки (подписка, порядок сообщений, SetActiveScene) и обход (Листинг 3.30 = SceneGraph) → обозреватель (3.31, Рисунок 3.10) → CaptureSnapshot → финал. Решения: тонкость запуска в абзаце загрузки; предложение про метку-признак SceneNode выкинуто; отсылка вперёд оформлена «в пункте 3.2.4». Листинги 3.29/3.30 поменялись местами (реестр обновлён).
11. Финальные ручные правки Макса (2026-06-05, после пересборки): MainMenuPanel «запускает открытие сцен VR-редактора и песочницы»; в загрузке – «сохранённые трансформации и, при наличии, позы костей», предложение про второй проход родительства УБРАНО; обозреватель «внутри префаба UserPanel», «выбору/выделению объектов сцены», отсылка вперёд «в пункте 3.2.6» (выделение/кости); CaptureSnapshot без ссылки на 3.1.4; финал дополнен «прямой выход из приложения не зафиксирует правки». ХВОСТЫ к закрытию при приёмке: (а) оборванное предложение в абзаце перестройки «однако измененик дочерне-родительских отношений не .»; (б) опечатки «сфоцств» → «свойств», «описываются пункте» → «описываются в пункте»; (в) согласование «обоих статичных UI панелей» → «обеих»; (г) решить, нужна ли фраза о втором проходе родительства (второй цикл в Листинге 3.30 остался без пояснения).
12. Хвосты закрыты (2026-06-05): (а)–(в) поправил Макс («в UI смена родителя не реализована», «инспекторе свойств», «в пункте 3.2.6», «обеих»); (г) добавлена фраза о втором проходе родительства с оговоркой «механизм заложен в формат данных и загрузку, однако интерфейсом пока не задействован» – согласуется с (а) и правилом 6.3 (нереализованное не выдавать за готовое). Пункт готов к финальной вычитке Макса.
