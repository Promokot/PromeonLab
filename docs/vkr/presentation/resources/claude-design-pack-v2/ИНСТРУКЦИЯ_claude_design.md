# Инструкция для Claude Design — реструктуризация деки PromeonLab (вставить промптом)

> Вставь ЭТОТ текст сообщением в чат. Файлы пака (`база_дека.html`, `план_реструктуризации.md`,
> всё из `assets/`) приложи к тому же сообщению.

---

Ты — дизайнер презентаций. У меня уже есть готовый слайд-дек к защите ВКР в фирменном стиле
РТУ МИРЭА (Институт кибербезопасности, КБ-9) — файл **`база_дека.html`** (формат `deck-stage`,
1920×1080, один HTML-файл, бренд и CSS уже внутри). **Не начинай с нуля — бери эту деку как основу
и перестрой её** по приложённому плану.

**Задача:** применить реструктуризацию из **`план_реструктуризации.md`** — дека вырастает
с 14 до **17 слайдов**. Сохрани существующий стиль, CSS-классы и бренд-разметку `база_дека.html`;
меняй только структуру и контент слайдов согласно плану.

**Результат:** один обновлённый HTML-файл того же формата `deck-stage` (1920×1080), русский язык,
17 секций `<section>`. Заметки докладчика — в `<script id="speaker-notes">` (JSON-массив, по строке
на слайд), как в исходнике.

## Бренд — строго (всё уже задано в CSS `база_дека.html`, не ломать)

- Фон белый. Палитра: синий МИРЭА `#355CA8`/`#3772B3`/`#5FC1CF`; бирюза ИКБ
  `#006A81`/`#004352`/`#94CCD9`; серый `#E6E6E6`. Цвета вне палитры нельзя.
- Шрифт Helvetica Neue (Arial запасной). Заголовки — Bold тёмно-синий; подзаголовки обязательны.
- Логотип-круг РТУ МИРЭА — правый верхний угол (мелко на контентных слайдах). Декор-сферы — бледно
  по краям титула/заключения. Не выдумывай новые знаки — используй те, что уже в деке.
- Карточки — скруглённые углы + тонкий бирюзовый контур; ключевые термины — бирюзой Bold;
  номер слайда — внизу справа.
- Быстрый визуальный ориентир по стилю — `assets/ref_ivanova_examples_collage.png`,
  палитра — `assets/palette_swatch.png`.

## Что именно менять — см. `план_реструктуризации.md` (слайд-за-слайдом)

Краткая карта изменений:
1. **Титул** — ФИО полностью «Пищагин Максим Антонович»; заголовок темы перекрасить в бирюзу ИКБ
   `#006A81`; слоган «ВМЕСТЕ СОЗДАЁМ БУДУЩЕЕ» привести к формату шаблона (капитель, разрядка,
   приглушённый серый `#8291A2`, внизу слева).
4. **2.2 Аналитический** — переписать выводы (5 тезисов от общего к частному, текст в плане);
   стек технологий — **одной вертикальной колонкой** карточек с логотипами (не сеткой).
5. **3. Постановка задачи** — цепочку из 5 этапов переместить вниз и **убрать стрелки** между
   карточками; убрать тезис об «автономной работе без ПК».
6. **4. Функциональная схема** — заменить картинку на `assets/diagram_functional_v2.svg`; убрать
   слово «конвейер»/«направленный»; добавить, что 5 подсистем = 5 областей задачи + 2 сквозные
   прослойки (UI и VR-взаимодействие — они уже на схеме).
7–8. **Информационное обеспечение — 2 слайда** (что храним / как храним; крупный JSON на втором).
9–12. **Программное обеспечение — 4 слайда** (каркас / сцена-объекты-риги / анимация / экспорт).
13. **Техническое обеспечение** — 1 слайд (на чём протестировано: Quest 2/3/3s).
14–17. Видео · план развития · **заключение (отдельный слайд)** · **спасибо (отдельный слайд)**.

> В плане есть два пункта с пометкой ⚠️ (строка «Автономность» в таблице слайда 3 и пункт
> «автономная сборка» в плане развития) — следуй тому, что выбрал автор; по умолчанию убери оба,
> чтобы дека себе не противоречила.

## Карта «слайд → картинка» (вставлять как есть)

| Слайд | Файл |
|---|---|
| 4 — стек | логотипы из `stack-logos/` (см. `stack-logos/СКАЧАТЬ_логотипы.md`); если файлов нет — аккуратные карточки-заглушки с названием |
| 6 — функц. схема | `assets/diagram_functional_v2.svg` |
| 7 — что храним | `assets/fig_3.2_data_structures.png` |
| 8 — как храним | `assets/fig_3.1_storage_tree.png` + код `assets/scene_example_large.json` (и опц. `assets/animation_example.json`) |
| 9 — каркас | `assets/diagram7_framework.png` |
| 10 — сцена/объекты/риги | 2–3 из: `fig_3.11_model_proxy_bones`, `fig_3.7_scene_outliner`, `fig_3.10_proxy_rig_structure`, `fig_3.8_asset_browser`, `fig_3.13_gizmo` |
| 11 — анимация | `assets/fig_3.14_animator_panel.png` |
| 12 — экспорт | `assets/fig_3.15_export_panel.png` |
| 3 — скриншоты аналогов · 14 — видео | оставить аккуратные рамки-заглушки (подменю вручную) |

> Картинки, которые уже встроены в `база_дека.html` (бренд-сферы, логотип, существующие рисунки),
> лежат в её папке `images/` — не трогай их. Новые файлы (`diagram_functional_v2.svg`, JSON-ы,
> новые рисунки 3.7/3.10/3.13/3.15 и логотипы стека) положи в ту же папку `images/`.

## Плейсхолдеры
- Логотипы стека (слайд 4), скриншоты аналогов (слайд 3), кадр видео (слайд 14) — если файла нет,
  оставь рамку-заглушку в бирюзовом контуре с подписью по центру.

## Заметки докладчика
- Перепиши под новую структуру (17 строк). Для слайда **«Заключение» заметку оставь пустой/паузой** —
  автор его в речи не озвучивает, сразу переходит к «Спасибо».
