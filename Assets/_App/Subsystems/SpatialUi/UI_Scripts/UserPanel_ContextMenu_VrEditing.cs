using UnityEngine;

// Context menu shown for AppMode.VrEditing and AppMode.Sandbox (modes share the same controls;
// Sandbox differs only in that its scene is never persisted).
// Populate with editing-specific controls in a future phase.
public class UserPanel_ContextMenu_VrEditing : MonoBehaviour { }
