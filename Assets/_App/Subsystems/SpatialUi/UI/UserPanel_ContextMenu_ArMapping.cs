using UnityEngine;

// Context menu placeholder shown when AppMode == ArMapping.
// Populate with AR mapping controls in a future phase.
public class UserPanel_ContextMenu_ArMapping : MonoBehaviour { }
